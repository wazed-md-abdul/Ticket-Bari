export const metadata = {
  title: "Available Commutes",
  description: "Browse and book available transport tickets on TicketBari.",
};

export default function TicketsLayout({ children }) {
  return children;
}
