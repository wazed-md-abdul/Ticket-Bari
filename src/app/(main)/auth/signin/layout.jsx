export const metadata = {
  title: "Sign In",
  description: "Sign in to your TicketBari account to buy tickets or list route schedules.",
};

export default function SignInLayout({ children }) {
  return children;
}
