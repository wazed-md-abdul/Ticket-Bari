export const metadata = {
  title: "Sign Up",
  description: "Create a new TicketBari account to start booking rides or listing tickets.",
};

export default function SignUpLayout({ children }) {
  return children;
}
