export const metadata = {
  title: "Admin Dashboard",
  description: "Admin panel for user controls, ticket approvals, and ad campaigns.",
};

export default function AdminLayout({ children }) {
  return children;
}
