export const metadata = {
  title: "Vendor Dashboard",
  description: "Vendor ticket management and revenue dashboard panel.",
};

export default function VendorLayout({ children }) {
  return children;
}
